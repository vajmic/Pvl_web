v12
- Caches both official PVL weekly and monthly PNG graphs.
- 24h always uses own interactive data.
- 7d uses own interactive history only after >=6.5 days and >=80 points; until then official PVL week graph.
- 30d uses own interactive history only after >=29 days and >=300 points; until then official PVL month graph.
- Once own history is dense enough, the switch happens automatically.
- This architecture can later extend to 90d / 1y views using accumulated cache.
